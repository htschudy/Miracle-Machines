class Item extends Phaser.GameObjects.Sprite{
    constructor(scene, x, y, id, frame){
        super(scene, x, y, frame);
        this.aspects = 0;
        this.


        scene.add.existing(this);
    }
    update(){

    }
}